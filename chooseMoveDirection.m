function moveDir = chooseMoveDirection(particles, weights, beaconLoc, beaconObs, currentTheta, dataStore)
% Chooses a world-frame heading to maximize information gain.

    moveDir = theta;
end
 
 
 
